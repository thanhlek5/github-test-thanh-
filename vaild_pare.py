def invalid(s):
    stack =[]
    for i in s:
s = "(){}[]"
print(invalid(s))
    return s1] != "}" or i == "[" and stack[i-1] != "]":
    pass
if not stack or i == ")" and stack[i-1] != "(" or i == "{" and stack[i-
        if i in "({[":
            stack.append(i)
        else:
            for j in range(len(stack)):
                if i == ")" :